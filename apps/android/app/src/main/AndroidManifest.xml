<?xml version="1.0" encoding="utf-8"?>
<!-- Plumbline is fully offline: no INTERNET permission is declared. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Plumbline"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Plumbline">

        <activity
            android:name="dev.plumbline.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenLayout|screenSize|smallestScreenSize|keyboardHidden|density"
            android:label="Plumbline"
            android:theme="@style/Theme.Plumbline">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
